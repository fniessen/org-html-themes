# Hey Emacs, this is a -*- org -*- file ...
#+TITLE:     Org mode theme preview
#+AUTHOR:    Fabrice Niessen
#+EMAIL:     (concat "fniessen" at-sign "pirilampo.org")
#+DESCRIPTION: A sample Org mode document for previewing and testing HTML themes.
#+KEYWORDS:  Org mode, HTML, theme, preview, demo
#+LANGUAGE:  en
#+OPTIONS:   H:6 num:nil toc:4

#+SETUPFILE: ~/org/setup/html-theme-readtheorg.setup

# See GitHub Settings > GitHub Pages > Theme Chooser

This document demonstrates how common Org mode elements are rendered by an HTML
theme. It includes headings, paragraphs, emphasis, source code, tables, lists,
definition lists, images, and other common constructs.

Text can be *bold*, /italic/, or +strikethrough+.

[[https://github.com/fniessen/org-html-themes/examples/org-mode-theme-preview.html][Link to another page.]]

There should be whitespace between paragraphs.

This paragraph demonstrates spacing between consecutive paragraphs.

* Level 1 heading

This paragraph is a normal paragraph following a header. It demonstrates how
regular body text is rendered by the selected HTML theme.

** Level 2 heading

This is a blockquote following a header.

When something is important enough, you do it even if the odds are not in your
favor.

*** Level 3 heading

**** Level 4 heading

- This is an unordered list following a header.
- This is an unordered list following a header.
- This is an unordered list following a header.

***** Level 5 heading

1. This is an ordered list following a header.
2. This is an ordered list following a header.
3. This is an ordered list following a header.

****** Level 6 heading

| Status  | Description   | Result |
|---------+---------------+--------|
| OK      | Sample value  | ✓      |
| Warning | Another value | !      |
| Error   | Example entry | ✗      |

**** Source code

#+begin_src javascript
// Javascript code with syntax highlighting.
var fun = function lang(l) {
  dateformat.i18n = require('./lang/' + l)
  return true;
}
#+end_src

#+begin_src ruby
# Ruby code with syntax highlighting
GitHubPages::Dependencies.gems.each do |gem, version|
  s.add_dependency(gem, "= #{version}")
end
#+end_src

*** There's a horizontal rule below this.

-----

*** Here is an unordered list:

- Item foo
- Item bar
- Item baz
- Item zip

*** And an ordered list:

1. Item one
2. Item two
3. Item three
4. Item four

*** And a nested list:

- level 1 item
  + level 2 item
  + level 2 item
    * level 3 item
    * level 3 item
- level 1 item
  + level 2 item
  + level 2 item
  + level 2 item
- level 1 item
  + level 2 item
  + level 2 item
- level 1 item

*** Small image example

Octocat

*** Large image example

Branching

*** Definition lists can be used with HTML syntax.

- Name :: Godzilla
- Born :: 1952
- Birthplace :: Japan
- Color :: Green

: Long, single-line code blocks should not wrap. They should horizontally scroll if they are too long. This line should be long enough to demonstrate this.

: The final element.
